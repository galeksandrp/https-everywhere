sudo: required
language: generic
services:
  - docker
script:
  - docker build -t httpse .
  - docker run --rm -ti -v $(pwd):/opt -w /opt httpse bash -c "LC_ALL=en_US.UTF-8 ./utils/generate-ruleset.py -i arbitrary.ch.list -o rules/Arbitrary.ch.xml"
  - git diff
